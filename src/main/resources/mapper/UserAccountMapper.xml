<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.takeout.mapper.UserAccountMapper">

    <select id="findByUsername" resultType="com.takeout.entity.UserAccount">
        SELECT username, password, role, profile_id as profileId
        FROM user_account WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.takeout.entity.UserAccount">
        INSERT INTO user_account (username, password, role, profile_id)
        VALUES (#{username}, #{password}, #{role}, #{profileId})
    </insert>
</mapper>
