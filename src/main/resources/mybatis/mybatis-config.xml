<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <settings>
        <!-- 驼峰命名自动映射 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
        <!-- 开启延迟加载 -->
        <setting name="lazyLoadingEnabled" value="true"/>
        <!-- 日志实现 -->
        <setting name="logImpl" value="LOG4J"/>
    </settings>

    <!-- 类型别名（逐个声明，避免中文路径下包扫描失败） -->
    <typeAliases>
        <typeAlias type="com.takeout.entity.Category" alias="Category"/>
        <typeAlias type="com.takeout.entity.ChatMessage" alias="ChatMessage"/>
        <typeAlias type="com.takeout.entity.ChatSession" alias="ChatSession"/>
        <typeAlias type="com.takeout.entity.Dish" alias="Dish"/>
        <typeAlias type="com.takeout.entity.Order" alias="Order"/>
        <typeAlias type="com.takeout.entity.OrderItem" alias="OrderItem"/>
        <typeAlias type="com.takeout.entity.UserAccount" alias="UserAccount"/>
        <typeAlias type="com.takeout.entity.UserProfile" alias="UserProfile"/>
    </typeAliases>

    <!-- 分页插件 -->
    <plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor">
            <property name="helperDialect" value="mysql"/>
            <property name="reasonable" value="true"/>
        </plugin>
    </plugins>
</configuration>
